name: NimbusPerms
version: '${version}'
main: dev.kryonix.nimbus.perms.NimbusPermsPlugin
description: Permissions bridge for Nimbus cloud networks — builtin or LuckPerms provider
author: Kryonix
depend:
  - NimbusSDK
folia-supported: true
softdepend:
  - LuckPerms
