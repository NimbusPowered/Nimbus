name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version (e.g. 0.2.0) — leave empty to use gradle.properties'
        required: false
        type: string
      prerelease:
        description: 'Mark as pre-release'
        required: false
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  build-and-release:
    name: Build & Create Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Determine version
        id: version
        run: |
          if [ -n "${{ inputs.version }}" ]; then
            VERSION="${{ inputs.version }}"
          else
            VERSION=$(grep 'nimbusVersion=' gradle.properties | cut -d'=' -f2 | tr -d '\r\n')
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "tag=v$VERSION" >> "$GITHUB_OUTPUT"
          echo "Version: $VERSION"

      - name: Build project
        run: ./gradlew shadowJar

      - name: Prepare release assets
        run: |
          VERSION="${{ steps.version.outputs.version }}"

          # Controller JAR (nimbus-core)
          CORE_JAR=$(find nimbus-core/build/libs -name "*-all.jar" | head -1)
          cp "$CORE_JAR" "nimbus-core-${VERSION}.jar"

          # Agent JAR (nimbus-agent)
          AGENT_JAR=$(find nimbus-agent/build/libs -name "*-all.jar" | head -1)
          cp "$AGENT_JAR" "nimbus-agent-${VERSION}.jar"

          echo "Controller: nimbus-core-${VERSION}.jar ($(du -h "nimbus-core-${VERSION}.jar" | cut -f1))"
          echo "Agent: nimbus-agent-${VERSION}.jar ($(du -h "nimbus-agent-${VERSION}.jar" | cut -f1))"

      - name: Create Release Draft
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: Nimbus ${{ steps.version.outputs.tag }}
          draft: true
          prerelease: ${{ inputs.prerelease }}
          generate_release_notes: true
          files: |
            nimbus-core-${{ steps.version.outputs.version }}.jar
            nimbus-agent-${{ steps.version.outputs.version }}.jar
          body: |
            ## Nimbus ${{ steps.version.outputs.tag }}

            ### Installation

            **Controller (Linux/macOS):**
            ```bash
            curl -fsSL https://raw.githubusercontent.com/${{ github.repository }}/main/install.sh | bash
            ```

            **Controller (Windows):**
            ```powershell
            irm https://raw.githubusercontent.com/${{ github.repository }}/main/install.ps1 | iex
            ```

            **Agent (Linux/macOS):**
            ```bash
            curl -fsSL https://raw.githubusercontent.com/${{ github.repository }}/main/install-agent.sh | bash
            ```

            **Agent (Windows):**
            ```powershell
            irm https://raw.githubusercontent.com/${{ github.repository }}/main/install-agent.ps1 | iex
            ```

            ### Changes
